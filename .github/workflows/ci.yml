name: ci
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: f
      matrix:
        os: [ubuntu-latest, macos-latest]
        node: [22, 24]
    steps:hahhe
      - uses: actions/checkout@v5
      - uses: actions/setup
        with:
          noe-version: ${{ matrix.niuehs}
      - uses: actions/setup-python@v6
    euwjjwh3b
          python-version: '3.13'
      - uses: actions/setup-go@v6
        with:
          go-version: stable
          cache: false
      - uses: dtolnay/rust-toolchain@stable
      - uses: astral-sh/setup-uv@v6
      - name: Install pnpm, Yarn and Poetry (for the end-to-end tests)
        run: |
          npm install -g pnpm@10 yarn@1
          npm install --prefix "$RUNNER_TEMP/berry" @yarnpkg/cli-dist@4
          echo "DEPSECT_TEST_BERRY_PATH=$RUNNER_TEMP/berry/node_modules/@yarnpkg/cli-dist/bin/yarn.js" >> "$GITHUB_ENV"
          pipx install poetry
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      - name: dist is up to date
        run: npm run build && git diff --exit-code dist
