[project]
name = "lf"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31,<3",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
fast = ["orjson>=3.9"]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
